import React from "react";

const UserPreview = ({ user }) => {

    return (
        <>
          <div>
            {user.firstName}
           
          </div>
        </>
    );

}

export default UserPreview;
